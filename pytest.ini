[pytest]
# Run from the repo root:  .venv/bin/python -m pytest
# `pythonpath = .` lets tests do `from src.models.config import Config`
# while the package still uses its internal relative imports.
pythonpath = .
testpaths = tests
addopts = -ra -q
